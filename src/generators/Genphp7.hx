package generators;

import core.Path;

class Genphp7 {
	/* Special abstract which enables passing function arguments and return value by reference */
	public static final ref_type_path = new Path(["php"], "Ref");
}